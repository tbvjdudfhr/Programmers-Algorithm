SELECT FH.FLAVOR
FROM FIRST_HALF AS FH
JOIN (
    SELECT FLAVOR, SUM(TOTAL_ORDER) AS JTO
    FROM JULY
    GROUP BY FLAVOR
) AS JJ ON FH.FLAVOR = JJ.FLAVOR
ORDER BY (JJ.JTO + FH.TOTAL_ORDER) DESC
LIMIT 3